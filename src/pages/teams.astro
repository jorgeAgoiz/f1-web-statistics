---
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import Layout from '../layouts/Layout.astro';

const response = await fetch("http://localhost:3012/teams", {
  method: "GET",
  headers: {
    "Content-Type": "application/json",
    "api-key": import.meta.env.BACKEND_API_KEY,
  },
});
const teams = await response.json()
console.log(teams)
---

<Layout title="Bienvenido a la página de estadisticas de Formula 1">
	<Header textTitle='Equipos' />
    {
        teams.length > 0
            ? teams.map(team => <h1>{team.name}</h1>)
            : null
    }
    <Footer />
</Layout>

<style>

</style>